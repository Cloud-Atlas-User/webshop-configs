# Actuator Endpoint Configurations
info: # /actuator/info
  app:
    description: "A service to maintain user information."

eureka:
  instance:
    metadata-map:
      onboardingUrl: http://localhost:${server.port}/mt/v1.0/subscriptions/tenants/{tenantId}
      onboardingGetDependenciesUrl: http://localhost:${server.port}/mt/v1.0/subscriptions/dependencies